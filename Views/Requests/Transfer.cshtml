@model AspnetCoreMvcStarter.Models.Request

@{
  ViewData["Title"] = "Transfer Request";
}

<h2>Transfer Request</h2>

@if (ViewBag.Assignees == null)
{
  <p class="text-danger">⚠ No available users for transfer.</p>
}
else
{
  <form asp-action="Transfer" asp-controller="Requests" method="post">
    <input type="hidden" asp-for="RequestId" />

    <div class="form-group">
      <label>Select New Requestor</label>
      <select name="newRequestorId" class="form-control" required>
        <option value="">-- Select User --</option>
        @foreach (var user in ViewBag.Assignees as SelectList)
        {
          <option value="@user.Value">@user.Text</option>
        }
      </select>
    </div>

    <button type="submit" class="btn btn-primary">Transfer</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
  </form>
}
